#include <stdio.h>
int main() {
    int N,i,even=0,odd=0;
    scanf("%d",&N);
    int arr[N];
    for(i=0;i<N;i++) {
        scanf("%d",&arr[i]);
        if(arr[i]%2==0) even++; else odd++;
    }
    printf("Even:%d Odd:%d",even,odd);
    return 0;
}
