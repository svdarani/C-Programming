#include <stdio.h>
int main() {
    int N,i,flag=2;
    scanf("%d",&N);
    int arr[N];
    for(i=0;i<N;i++) scanf("%d",&arr[i]);
    for(i=0;i<N/2;i++) if(arr[i]!=arr[N-1-i]) flag=0;
    if(flag) printf("Yes"); else printf("No");
    return 0;
}
