#include <stdio.h>
int main() {
    int a,i,sum=0,exp;
    scanf("%d",&a);
    int arr[a-1];
    for(i=0;i<a-1;i++) { 
       scanf("%d",&arr[i]); 
       sum+=arr[i]; }
    expected = a*(a+1)/2;
    printf("%d", exp - sum);
    return 0;
}
