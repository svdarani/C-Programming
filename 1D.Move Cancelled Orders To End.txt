#include <stdio.h>
int main() {
    int n,i,j=0;
    scanf("%d",&n);
    int arr[n],res[n];
    for(i=0;i<n;i++) scanf("%d",&arr[i]);
    for(i=0;i<n;i++) if(arr[i]!=0) res[j++]=arr[i];
    while(j<n) res[j++]=0;
    for(i=0;i<n;i++) printf("%d ",res[i]);
    return 0;
}
