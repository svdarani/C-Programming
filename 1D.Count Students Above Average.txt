#include <stdio.h>
int main() {
    int n,i,sum=0,count=0;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++) { scanf("%d",&arr[i]); sum+=arr[i]; }
    float avg=(float)sum/n;
    for(i=0;i<n;i++) if(arr[i]>avg) count++;
    printf("%d",count);
    return 0;
}
